const React = require('react');

function PlayGround() {
	return (
		<p className="tooltip">Hover over me
		  <span className="tooltiptext">Tooltip text</span>
		</p>
	)
}

module.exports = PlayGround;